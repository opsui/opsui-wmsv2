# ============================================================================
# WMS Backend Environment Configuration
# ============================================================================
#
# PRODUCTION SERVER: ssh root@103.208.85.233
# Database runs directly on server (NO Docker)
#
# LOCAL DEVELOPMENT:
#   SSH tunnel required: ssh -f -N -L 5433:localhost:5432 root@103.208.85.233
#   Then connect to localhost:5433 (tunneled to remote:5432)
#
# ============================================================================

# Node Environment
NODE_ENV=development

# Server Configuration - LOCKED PORT
PORT=3001  # 🔒 LOCKED - Backend API (never change)
HOST=0.0.0.0

# Database Configuration
# Connects via SSH tunnel: localhost:5433 → 103.208.85.233:5432
DB_HOST=localhost
DB_PORT=5433
DB_NAME=wms_db
DB_USER=wms_user
DB_PASSWORD=wms_password
DB_SSL=false
DB_POOL_MIN=2
DB_POOL_MAX=10

# Redis Configuration (optional - degrades gracefully if not available)
# NOTE: If Redis is on production server, tunnel: ssh -L 6379:localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_TTL=3600

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt Configuration
BCRYPT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000

# CORS Configuration
# Comma-separated list of allowed origins
# Production example: https://opsui.app,https://www.opsui.app
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=debug
LOG_FILE=logs/wms.log

# WebSocket Configuration - LOCKED PORT
WS_PORT=3002  # 🔒 LOCKED - WebSocket Server (never change)

# Application Configuration
DEFAULT_PICKER_CAPACITY=5
MAX_ORDERS_PER_PICKER=10
PICK_TIMEOUT_MINUTES=30

# Health Check
HEALTH_CHECK_INTERVAL=30000

# Feature Flags
ENABLE_WEBSOCKET=true
ENABLE_REDIS_CACHE=false
ENABLE_AUDIT_LOG=true

# Test Mode Configuration
# WARNING: Only enable in development/test environments, NEVER in production
TEST_MODE=false
TEST_API_KEY=

# ============================================================================
# Notification System Configuration
# ============================================================================

# Email Provider (SendGrid - Primary)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@wms.local
SENDGRID_REPLY_TO_EMAIL=

# Email Provider (Postmark - Fallback)
POSTMARK_API_KEY=
POSTMARK_FROM_EMAIL=noreply@wms.local
POSTMARK_REPLY_TO_EMAIL=

# Email Provider (AWS SES - Backup)
AWS_SES_REGION=us-east-1
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=
AWS_SES_FROM_EMAIL=noreply@wms.local

# SMS Provider (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_RATE_LIMIT=5

# Push Notifications (Web Push VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@wms.local

# Firebase Cloud Messaging (Optional - for push notifications)
GCM_API_KEY=
