{
  "routeSignatures": {
    "/packing": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In",
      "System Administratoradmin@wms.local",
      "Warehouse",
      "Pending",
      "All Priority",
      "Report Problem"
    ],
    "/stock-control": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In"
    ],
    "/maintenance": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin"
    ],
    "/bin-locations": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/production": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In",
      "Sign In"
    ],
    "/location-capacity": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In"
    ],
    "/orders": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In",
      "Sign In",
      "System Administratoradmin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/sales": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/cycle-counting": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In"
    ],
    "/business-rules": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/search": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In",
      "Sign In"
    ],
    "/waves": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/user-roles": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/role-settings": [
      "admin@wms.local",
      "Report Problem",
      "Settings",
      "Logout",
      "Back to Dashboard",
      "Sign In"
    ],
    "/developer": [
      "Back to App",
      "Overview",
      "Feature Flags",
      "Monitoring",
      "Data Management",
      "Back to App",
      "Overview",
      "Feature Flags",
      "Monitoring",
      "Data Management",
      "Sign In",
      "Back to App",
      "Overview",
      "Feature Flags",
      "Monitoring",
      "Data Management"
    ],
    "/reports": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/integrations": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In",
      "Sign In"
    ],
    "/zones": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/slotting": [
      "admin@wms.local",
      "Operations",
      "Inventory",
      "Warehouse",
      "Admin",
      "Sign In"
    ],
    "/login": [
      "Sign In",
      "Sign In"
    ],
    "/dashboard": [
      "Sign In",
      "Sign In"
    ],
    "/exceptions": [
      "Sign In"
    ],
    "/inwards": [
      "Sign In",
      "Sign In",
      "Sign In",
      "Sign In"
    ],
    "/rma": [
      "Sign In",
      "Sign In",
      "Sign In"
    ]
  },
  "elementBehaviors": {
    "button:has-text(\"admin@wms.local\")": "click",
    "button:has-text(\"Operations\")": "click",
    "button:has-text(\"Inventory\")": "click",
    "button:has-text(\"Warehouse\")": "click",
    "button:has-text(\"Admin\")": "click",
    "button:has-text(\"Reports\")": "click",
    "button:has-text(\"Settings\")": "click",
    "button:has-text(\"Dev Panel\")": "click",
    "button:has-text(\"Report Problem\")": "click",
    "button:has-text(\"Logout\")": "click",
    "button:has-text(\"Back to Dashboard\")": "click",
    "button:has-text(\"Role Switcher\")": "click",
    "button:has-text(\"Appearance\")": "click",
    "button:has-text(\"Notifications & Soun\")": "click",
    "button:has-text(\"Back to App\")": "click",
    "button:has-text(\"Overview\")": "click",
    "button:has-text(\"Feature Flags\")": "click",
    "button:has-text(\"Monitoring\")": "click",
    "button:has-text(\"Data Management\")": "click",
    "button:has-text(\"Testing Tools\")": "click",
    "button:has-text(\"Error Crawler\")": "click",
    "button:has-text(\"Refresh\")": "click",
    "button:has-text(\"Run Crawler\")": "click",
    "button:has-text(\"Dashboard\")": "click",
    "button:has-text(\"Sign In\")": "click",
    "button:has-text(\"Stop\")": "click",
    "button:has-text(\"System Administrator\")": "click",
    "button:has-text(\"Pending\")": "click",
    "button:has-text(\"All Priority\")": "click"
  },
  "commonPatterns": [
    "Navigation items are implemented as <button> elements, not <a> tags.",
    "Selectors utilize Playwright-style :has-text() pseudo-class.",
    "Menu labels follow standard Title Case convention.",
    "Global navigation is persistent across the /packing route.",
    "Text-based button selection is highly reliable (relevance: 1) for navigation",
    "Flat navigation structure using distinct labels for business domains",
    "User identification via email address displayed as a button",
    "Universal use of <button> tags for navigation",
    "High reliability (rel:1) of :has-text() selectors in current state",
    "Grouping of functional areas (Ops, Inventory, Warehouse) vs System areas (Admin, Reports, Settings)",
    "Selector Strategy: High reliance on text-based Playwright selectors (button:has-text('Label')).",
    "Navigation Structure: Flat list of top-level functional domains.",
    "Action Uniformity: All observed interactions are simple clicks.",
    {
      "pattern": "text_based_navigation",
      "usage": "Locating buttons",
      "example": "button:has-text('Operations')",
      "reliability": "High for static labels"
    },
    {
      "pattern": "flat_button_structure",
      "usage": "Menu layout",
      "description": "Navigation elements appear to be direct button descendants rather than nested lists."
    },
    "Global navigation menu containing Operations, Inventory, Warehouse, Admin, Reports, Settings",
    "User profile indicator displaying current email",
    "Consistent use of <button> tags for navigation actions",
    {
      "pattern": "Text-based Button Navigation",
      "description": "Primary navigation relies on button elements with visible text labels.",
      "usage": "High frequency for menu traversal"
    },
    {
      "pattern": "Role-based Grouping",
      "description": "Buttons are grouped by functional area (Inventory, Warehouse, Admin).",
      "usage": "Structuring test suites by module"
    },
    "Navigation elements are consistently identified using exact text matching within button tags (e.g., button:has-text('Inventory')).",
    "High-level categorization (Operations, Inventory, Warehouse, Admin) suggests a modular application structure.",
    "User-specific context (admin@wms.local) is exposed as a clickable element in the navigation area.",
    "Text-based navigation: All primary actions are triggered by buttons with distinct text labels.",
    "High Reliability: Selectors using `has-text` show 100% relevance (rel:1) for module names.",
    "Static Module List: The core modules (Operations, Inventory, Warehouse, Admin) appear consistently.",
    "All navigation elements are buttons with text-based selectors",
    "Consistent selector pattern: button:has-text('text')",
    "All navigation elements have click interaction",
    "High reliability (rel:1) for text-based selectors on this page",
    "Navigation structure includes user profile (admin@wms.local) and functional areas",
    "Text-based selectors are highly reliable (Rel: 1) for navigation buttons.",
    "Buttons use distinct, human-readable labels (e.g., 'Operations', 'Inventory').",
    "Uniform element type (button) for primary navigation.",
    "Selector Strategy: button:has-text('Exact Label') is 100% reliable for static menu items.",
    "Semantic Grouping: Navigation items are consistently implemented as <button> elements.",
    "button:has-text('Label')",
    "Functional grouping (Operations, Inventory, Warehouse, Admin)",
    "User profile trigger (admin@wms.local) as a button",
    "Navigation elements are strictly buttons (not <a> tags)",
    "Selectors rely on visible text content (e.g., button:has-text('Operations'))",
    "Menu items are flat in the observation list, likely representing the main app shell",
    "Text-based identification is highly reliable for both user profile and module names.",
    "Navigation elements are grouped by function (Operations, Warehouse, Admin).",
    "All interactive elements are standard HTML buttons.",
    "Text-based selectors are highly consistent (100% relevance)",
    "Primary interaction model relies on distinct button labels",
    "Settings categorized into distinct functional areas (Appearance, Notifications, Roles)",
    {
      "name": "TextBasedNavigation",
      "pattern": "button:has-text('{Label}')",
      "usage": "Primary method for identifying navigation elements",
      "reliability": "High (freq: 1, rel: 1 for all observed)"
    },
    {
      "name": "ToolGrouping",
      "pattern": "Cluster of buttons representing distinct dev-tools (Flags, Monitoring, Data, Testing, Errors)",
      "usage": "Verifying integrity of the developer menu"
    },
    "text-based selector usage (button:has-text)",
    "grouped navigation modules",
    "consistent button role for navigation",
    "Text-based visibility: All navigation elements are reliably identified by their visible text labels.",
    "Role consistency: Navigation items use the <button> role.",
    "Grouping: Elements (Operations, Inventory, Warehouse, Admin) appear as a cohesive navigation cluster.",
    "text_based_selection",
    "button_primary_interaction",
    "dashboard_menu_structure",
    "Uniform element type: All interactive elements are buttons.",
    "Text-based identification: Selectors rely heavily on visible labels ('Operations', 'Inventory').",
    "Module-based navigation: Clear segmentation of functional areas (Warehouse, Reports, Settings).",
    "Text-based selectors (`button:has-text(...)`) are highly reliable for this navigation bar.",
    "Navigation elements are consistently implemented as <button> tags.",
    "The user profile is identified by an email address format.",
    "PascalCase naming convention for menu items",
    "Flat list of top-level navigation categories",
    "Selector Strategy: button:has-text('Label') is highly reliable (rel:1) for static menu items.",
    "UI Structure: Top-level navigation bar present on the target route.",
    "Interaction: All observed elements are clickable triggers.",
    {
      "pattern": "Text-based Button Selection",
      "usage": "Use `button:has-text('Exact Name')` for high-reliability targeting of navigation items.",
      "reliability": "High (rel: 1)"
    },
    {
      "pattern": "Static Navigation Grouping",
      "usage": "Navigation items (Operations, Inventory, etc.) appear consistently as a group.",
      "reliability": "High"
    },
    "Selector strategy relies heavily on Playwright's `:has-text()` pseudo-class targeting button elements.",
    "Direct text matching is used for all navigation items.",
    {
      "pattern": "Text-based Navigation",
      "description": "Primary navigation relies on `button:has-text('Label')` selectors.",
      "usage": "Used for all top-level menu items (Operations, Inventory, etc.)."
    },
    {
      "pattern": "Role-based Visibility",
      "description": "Presence of 'Admin' and 'Dev Panel' indicates the current test context has elevated privileges.",
      "usage": "Tests must ensure the user is logged in as an admin to access these elements."
    },
    "Text-based selectors using :has-text('Label') are highly reliable (Rel: 1)",
    "Navigation items are consistently button elements",
    "Labels are capitalized proper nouns (e.g., 'Operations', 'Inventory')",
    {
      "pattern": "text_based_button_selection",
      "description": "Buttons are consistently identified using explicit text content (e.g., has-text).",
      "example": "button:has-text('Run Crawler')"
    },
    {
      "pattern": "functional_grouping",
      "description": "Clear separation between navigation tools (Feature Flags, Monitoring) and runtime actions (Refresh, Run)."
    },
    "Text-based selectors for primary actions (has-text)",
    "Explicit 'Sign In' button indicates entry point",
    "Primary action buttons are reliably identified using text content (e.g., 'Sign In').",
    "Login routes follow a single-action trigger pattern for submission.",
    {
      "pattern": "AuthGate",
      "description": "Protected routes display a Sign In prompt when accessed without a valid session."
    },
    "AuthGate",
    "TextBasedIdentification",
    "Text-Based_Selector_Strategy (has-text)",
    "Text-based button selection using Playwright/Puppeteer style :has-text() pseudo-class",
    "Direct click interaction for authentication entry points",
    "Use text-based selectors (e.g., has-text) for primary action buttons.",
    "Exception routes redirect to authentication entry points.",
    "Text-based button selection (button:has-text)",
    "Authentication gating before accessing main application features",
    "Use of explicit text-based selectors (button:has-text('...')) for primary actions",
    "Direct click actions on entry pages",
    "Playwright-style text selectors (e.g., has-text)",
    "Direct user action simulation on buttons",
    "Use of Playwright's `has-text()` pseudo-class for identifying elements with visible text.",
    "Role-based targeting (button) combined with text content.",
    "Auth_Gate_Pattern",
    "Text_Content_Selector_Strategy",
    "text_based_button_selectors",
    "action_buttons_containing_verb_noun_pairing",
    "Text-based button selection",
    "Authentication controls present on functional pages",
    "Text-based selectors (has-text) are reliable for primary actions like authentication.",
    "Text-based selectors for authentication buttons (e.g., :has-text('Sign In'))",
    "Authentication gating on protected routes",
    "Text-based button selectors (:has-text)",
    "Explicit action verbs (Sign In)",
    "High relevance (rel:1) indicates critical path element",
    "Text-based role selection (e.g., button:has-text)",
    "Identification of interactive elements via visible text content (has-text).",
    "Standard click interaction for primary actions.",
    "Text-based selectors using :has-text() pseudo-class",
    "Direct interaction with labeled buttons",
    "Playwright-style text selectors (e.g., :has-text())",
    "Explicit action labeling (Sign In) for primary workflow buttons",
    "Direct interaction flows (Click -> Action)",
    "Text-based selectors (e.g., :has-text()) are highly effective for identifying interactive elements.",
    "Authentication workflows are initiated via distinct 'Sign In' buttons on the main route.",
    "Use of Playwright/Cypress style 'has-text' pseudo-class for selectors",
    "Text-based targeting for high-level navigation elements",
    "Use text-based selectors (has-text) for distinct action buttons like 'Sign In'.",
    "Primary action buttons are often the entry point for authentication flows.",
    "Authentication gating on specific routes",
    "Explicit action (click) required to initiate authentication",
    "Use of text-based selectors (has-text) for identifying primary action buttons.",
    "Authentication gatekeeping on specific feature routes.",
    "Use of :has-text() pseudo-class for high-relevance, user-facing elements.",
    "Direct text matching for primary call-to-action buttons (e.g., 'Sign In').",
    "semantic_role_with_text_filtering",
    "high_relevance_text_selectors",
    "selector:text-based",
    "Text-based selectors (:has-text) for identifying primary actions",
    "Explicit 'Sign In' access control on feature routes",
    "Text-based selectors for primary navigation actions",
    "Authentication gating on protected resources",
    "Text-based selectors (e.g., has-text) are reliable for primary actions.",
    "Critical pages (Business Rules) are gated behind authentication flows.",
    "Primary actions are triggered via buttons with explicit text labels.",
    "Selectors rely on visible text content (e.g., has-text).",
    "Text-based selectors for buttons (e.g., :has-text('Sign In'))",
    "Direct interaction flow (Click -> Navigate/Auth)",
    "Text-based selectors (has-text) for primary action buttons",
    "Authentication gating on restricted routes",
    "High reliability on visible text content for element identification (100% frequency/relevance).",
    "Clear semantic grouping of buttons: Navigation vs. Control Actions.",
    "Consistent use of Title Case for button labels.",
    "Text-based selector strategy (has-text)",
    "Flat list of navigation triggers",
    "Explicit role labeling (e.g., System Administrator)",
    "Text-based selectors: `button:has-text('Label')`",
    "UI Grouping: Controls are likely grouped in a header or sidebar toolbar",
    "Interaction: All observed elements trigger state changes via click events",
    "Text-based button identification (has-text)",
    "Explicit button role usage",
    "Auth Gate: Protected routes display a Sign In button when accessed without a valid session.",
    "Text-based Selection: Using specific text content to locate primary action buttons.",
    "Buttons are reliably targeted using visible text content (has-text).",
    "Critical workflow actions are initiated via standard click events.",
    "use_has_text_for_buttons",
    "role_based_selection (button)",
    "action_oriented_labeling",
    "Use text-based selectors for authentication actions (e.g., button:has-text('Sign In')).",
    "Direct user interaction (click) is the primary trigger for integration setup.",
    "Text-based selectors are highly reliable for primary action buttons like authentication.",
    "Search pages often expose authentication triggers for unauthenticated users.",
    "Usage of Playwright-style pseudo-classes (e.g., :has-text)",
    "Direct text content matching for button identification"
  ],
  "antiPatterns": [
    "Hardcoding specific user credentials (admin@wms.local) in selectors creates non-reusable tests.",
    "Reliance on text content alone for selectors increases fragility during UI refactors or localization.",
    "Lack of data-testid or stable ID attributes on critical navigation elements.",
    "Avoid using structural selectors (nth-child, css hierarchy) when text is unique",
    "Avoid hardcoding specific user email ('admin@wms.local') in reusable test functions",
    "Do not assume 'Dev Panel' is present in non-development environments",
    "Hardcoding environment-specific data (e.g., 'admin@wms.local') in selectors",
    "Reliance on visible text copy which may change during localization or UI updates",
    "Lack of unique identifiers (IDs/Classes) forcing dependence on text content",
    "Hardcoded Credentials: Selecting elements by specific user email ('admin@wms.local') creates test fragility across different user accounts.",
    "Text Coupling: Relying strictly on button text (e.g., 'Operations') makes tests brittle to UI copy changes or localization.",
    {
      "pattern": "hardcoded_user_credentials",
      "reason": "Selector 'admin@wms.local' is environment/user specific and will break tests run by other users or in different environments."
    },
    {
      "pattern": "environment_specific_ui",
      "reason": "The 'Dev Panel' button likely appears only in non-production or debug modes; tests relying on it may fail in strict production builds."
    },
    "Hardcoding specific user credentials (email) in selectors",
    "Testing only navigation elements on a feature-specific route",
    "Using generic text labels like 'Admin' or 'Reports' without context for selectors",
    {
      "pattern": "Hardcoded Dynamic Data in Selectors",
      "description": "Using 'admin@wms.local' in a selector makes the test fail for any other user.",
      "risk": "Test flakiness across different user accounts"
    },
    {
      "pattern": "Generic Text Reliance",
      "description": "Relying solely on 'has-text' without a stable ID or class can break if UI labels change.",
      "risk": "Maintenance burden"
    },
    {
      "pattern": "Assuming Page Content",
      "description": "The observations only list navigation elements, not the actual 'Bin Locations' content grid/form.",
      "risk": "Incomplete test coverage"
    },
    "Avoid using partial text matches (e.g., :has-text('Op')) as 'Operations' and 'Orders' may create ambiguity.",
    "Avoid generic CSS selectors (e.g., .btn, button:nth-child(3)) as the menu order or classes might change.",
    "Do not assume 'Dev Panel' is visible for all user roles; it appears to be an environment or role-specific element.",
    "Hardcoded User Credentials: Using 'admin@wms.local' in selectors creates fragility; tests will fail for other users.",
    "Generic Indexing: Avoid using nth-child or generic button classes, as the specific text labels are available and unique.",
    "Brittle Dev Dependencies: Relying on 'Dev Panel' for critical path tests is risky as it may not exist in production environments.",
    "Relying solely on text-based selectors which may break with UI changes",
    "No stable attributes (data-testid, id) mentioned for element identification",
    "No explicit wait strategies for navigation completion",
    "No verification of successful navigation after clicks",
    "Avoid using CSS classes or structural selectors (nth-child) when visible text is available and unique.",
    "Avoid hardcoding dynamic IDs if they are not present in the observations.",
    "Do not assume all buttons result in immediate page navigation; some (like 'Admin' or 'Settings') may trigger dropdowns.",
    "Fragile Selectors: Using 'admin@wms.local' in text selectors is high-risk; user accounts change.",
    "Context Collision: Selecting buttons by text alone (e.g., 'Settings') may fail if the main content area contains a button with the same name.",
    "Using complex CSS selectors when simple text matches exist",
    "Hardcoding specific user email strings in selectors for general tests",
    "Relying on element order/index (nth-child)",
    "Relying solely on text content for selectors (susceptible to spelling changes or localization)",
    "Assuming direct navigation to /waves works without clicking the parent category (Operations/Warehouse)",
    "Hardcoding specific email 'admin@wms.local' in selectors (not dynamic to current user)",
    "Hardcoding specific user emails (e.g., 'admin@wms.local') in selectors, which breaks tests when running as different users.",
    "Using CSS indexers (e.g., :nth-child) for module selection, as the visibility/order of buttons may change based on role permissions.",
    "Assuming 'Dev Panel' is always present; this likely appears only for specific admin roles.",
    "Hardcoded user credentials in selectors (e.g., 'admin@wms.local')",
    "Exact match on truncated text (e.g., 'Notifications & Soun')",
    "Absence of semantic data attributes (e.g., data-testid) for stable testing",
    {
      "pattern": "Index-based selection",
      "reason": "Menu items (Overview, Feature Flags, etc.) could be reordered; text-based selectors are preferred."
    },
    {
      "pattern": "Assuming synchronous completion",
      "reason": "Actions like 'Run Crawler' likely trigger async jobs; tests should wait for status updates rather than immediate completion."
    },
    "hardcoded_user_credentials_in_selectors",
    "brittle_text_selectors_without_containment",
    "Hardcoded Indexing: Do not use nth-child or indexing for navigation items, as menu orders may change.",
    "Global Text Scoping: Avoid unscoped text selectors (e.g., just 'Admin') if the main content area might contain the word 'Admin'. Scope to the navigation container.",
    "Environment Specifics: Do not rely on 'Dev Panel' being present in testing or production environments.",
    "hardcoded_credentials_in_selector",
    "fragile_text_dependency",
    "Hardcoded User Data: Using 'admin@wms.local' as a selector makes tests fail for other users.",
    "Brittle Text Selectors: Relying solely on UI text makes tests fragile against spelling changes or internationalization.",
    "Production Dev Tools: Presence of 'Dev Panel' in production observations suggests a potential security or configuration risk.",
    "Hardcoding specific user data (e.g., 'admin@wms.local') in selectors makes tests fail when run as different users.",
    "Relying on 'Dev Panel' visibility in production environments (often hidden in prod).",
    "Assuming navigation order remains static if using index-based selectors (though text-based selectors mitigate this).",
    "Hardcoding dynamic user data (admin@wms.local) in selectors",
    "Using generic 'button' selectors without distinguishing text or roles",
    "Chaining clicks without waiting for network idle or route completion",
    "Hardcoded Credentials: Using specific email addresses (e.g., 'admin@wms.local') in selectors makes tests brittle if the logged-in user changes.",
    "Text Dependency: Relying strictly on visible text for selectors may break if UI labels are renamed (e.g., 'Operations' -> 'Ops').",
    {
      "pattern": "Hardcoded User Credentials in Selectors",
      "reason": "Using `admin@wms.local` in selectors creates environment dependency and fails for other test users."
    },
    {
      "pattern": "Assuming Dev Panel Availability",
      "reason": "The 'Dev Panel' button may not exist in production or staging environments."
    },
    "Hardcoded credentials in selectors (e.g., 'admin@wms.local'), causing test failures if the logged-in user changes.",
    "Global selection of buttons without a parent container context (e.g., sidebar or navbar), risking false positives if button text exists in the main content area.",
    "Assuming 'Dev Panel' is always present (environment-specific element).",
    {
      "pattern": "Hardcoded User Credentials in Selectors",
      "reason": "Using `button:has-text('admin@wms.local')` makes tests fail for different users or environments."
    },
    {
      "pattern": "Brittle Text Selectors",
      "reason": "Relying solely on visible text (`has-text`) for navigation causes failures if UI labels change or are internationalized."
    },
    {
      "pattern": "Missing Functional Scope",
      "reason": "Observations only cover global navigation. Testing '/zones' requires identifying specific picking elements (e.g., bins, pick lists), which are currently missing from the model."
    },
    "Hardcoding dynamic user data like 'admin@wms.local' in selectors",
    "Relying on specific user emails for stable test logic",
    {
      "pattern": "blind_action_execution",
      "description": "Clicking 'Run Crawler' without handling potential loading states or async completion.",
      "risk": "Race conditions or test flakiness."
    },
    {
      "pattern": "hardcoded_menu_order",
      "description": "Assuming buttons appear in a specific DOM order.",
      "risk": "Tests break if menu items are reordered."
    },
    "Clicking submit button without verifying presence of required input fields (username/password) first",
    "Assuming immediate page transition without handling network latency",
    "Avoid using generic 'button' selectors without text constraints, as login forms often contain secondary buttons (e.g., 'Cancel', 'Forgot Password').",
    "Do not rely on absolute positioning or dynamic classes for the submit button.",
    {
      "pattern": "SkippingAuthCheck",
      "description": "Attempting to assert order data elements immediately on /orders without first handling the Sign In button."
    },
    "DirectAccessWithoutAuth",
    "AssumingDataVisibility",
    "Assuming_Authenticated_State",
    "Hard-coding text content for selectors (brittle if localized)",
    "Testing dashboard state without checking authentication status first",
    "Avoid generic selectors (e.g., 'button') without text context on error pages.",
    "Do not assume static error messages; rely on interactive elements for stability.",
    "Attempting to interact with queue elements before handling the 'Sign In' state",
    "Using brittle selectors (e.g., nth-child) instead of semantic text or IDs for buttons",
    "Avoid relying on text content if localization is required",
    "Avoid using generic button selectors without text context if multiple buttons exist",
    "Relying solely on visible text for selectors (brittle if copy changes or localization is added)",
    "Assuming immediate navigation without waiting for network idle or URL changes",
    "Avoid using generic 'button' selectors without text filtering if multiple buttons are present.",
    "Avoid relying solely on text content if the application is expected to support internationalization (i18n).",
    "Brittle_Selector_Dependency",
    "ambiguous_text_selectors",
    "missing_parent_scope_context",
    "Using generic element types (e.g., 'button') without text filters",
    "Assuming authenticated state on /search route",
    "Avoid using generic 'button' selectors without text context if multiple buttons exist.",
    "Hard-coding 'Sign In' text without accounting for localization or 'Log In' variations",
    "Attempting to interact with order data before resolving the authentication prompt",
    "Using generic 'button' selectors without text content",
    "Assuming immediate state change without handling navigation",
    "Assuming authenticated state on /production route",
    "Using complex CSS selectors when semantic text is available",
    "Relying solely on text content ('Sign In') if the application supports multiple languages or dynamic text.",
    "Using generic 'button' selectors without text constraints (risk of selecting wrong button)",
    "Hard-coding dynamic attributes if text content is stable",
    "Avoid using generic 'button' selectors without text filtering",
    "Avoid relying on DOM order (e.g., nth-child) for action buttons",
    "Avoid using structural selectors (e.g., nth-child) when visible text is available and unique.",
    "Do not assume navigation is complete immediately after click; wait for URL or DOM changes.",
    "Hardcoded text selectors (susceptible to UI copy changes)",
    "Lack of semantic role attributes (e.g., relying solely on text 'Sign In')",
    "Do not assume the user is already authenticated when accessing /bin-locations.",
    "Avoid using dynamic classes or generic 'button' tags without text filters for this specific element.",
    "Attempting to interact with report data before verifying authentication state",
    "Hardcoding selectors without checking for auth gate presence first",
    "Attempting to interact with slotting data before handling the 'Sign In' overlay.",
    "Using generic 'Sign In' text selectors if multiple auth entry points exist (risk of ambiguity).",
    "Avoid using generic tag selectors (e.g., just 'button') without text constraints, as multiple buttons may exist.",
    "Avoid strict nth-child selectors if layout changes are frequent.",
    "brittle_css_selectors",
    "hardcoded_positional_selectors",
    "Hard-coding navigation without verifying authentication status",
    "Using fragile text selectors if UI localization is planned",
    "Attempting to interact with capacity data without handling the Sign In state",
    "Using brittle dynamic attributes (e.g., arbitrary IDs) instead of visible text for buttons",
    "Avoid using brittle CSS classes for the Sign In button when the text content is static and unique.",
    "Do not assume direct access to /business-rules content without handling the 'Sign In' state.",
    "Using generic tag selectors without text context (e.g., 'button' alone)",
    "Assuming the user is already authenticated on /zones",
    "Attempting to interact with role-management elements without first handling the 'Sign In' gatekeeper",
    "Avoid using complex CSS selectors (e.g., nth-child) when visible text is distinct and stable.",
    "Avoid assuming 'Refresh' or 'Stop' are instantaneous; they likely trigger asynchronous state changes requiring wait logic.",
    "Hard-coding specific text labels which may change during localization or UI updates",
    "Assuming all navigation buttons are visible for every user role (missing RBAC checks)",
    "Testing 'Logout' in the middle of a suite without re-authentication logic",
    "Avoid using CSS nth-child selectors as button order may change dynamically",
    "Avoid partial text matches (e.g., 'contains') if labels like 'Pending' and 'Pending Review' exist in the same context",
    "Direct Data Access: Attempting to interact with order list data before handling the Sign In prompt.",
    "State Assumption: Assuming the /orders route is publicly accessible without checking for the presence of the Sign In button first.",
    "Avoid using generic 'button' selectors without text filters, as multiple buttons may exist.",
    "Do not assume immediate page navigation without handling asynchronous load states.",
    "using_css_only_selectors_without_text_context",
    "hardcoding_dynamic_ids",
    "relying_on_positional_selectors (nth-child)",
    "Avoid using generic 'button' selectors without text context, as integration lists often contain multiple action buttons (Configure, Remove, Sign In).",
    "Do not assume static button states; 'Sign In' often changes to 'Connected' or 'Disconnect' post-action.",
    "Using generic selectors (e.g., 'button:first-child') when distinct text is available.",
    "Hard-coding specific CSS classes that may change during UI refactoring.",
    "Relying solely on visible text 'Sign In' which may change or be duplicated",
    "Lack of data-testid or stable ID attributes for critical buttons",
    "Ambiguous context: 'Sign In' on a settings page could imply a redirect or an impersonation feature"
  ],
  "selectorReliability": {
    "button:has-text(\"admin@wms.local\")": 1,
    "button:has-text(\"Operations\")": 1,
    "button:has-text(\"Inventory\")": 1,
    "button:has-text(\"Warehouse\")": 1,
    "button:has-text(\"Admin\")": 1,
    "button:has-text(\"Reports\")": 1,
    "button:has-text(\"Settings\")": 1,
    "button:has-text(\"Dev Panel\")": 1,
    "button:has-text(\"Report Problem\")": 0.7,
    "button:has-text(\"Logout\")": 0.7999999999999999,
    "button:has-text(\"Back to Dashboard\")": 0.6,
    "button:has-text(\"Role Switcher\")": 0.6,
    "button:has-text(\"Appearance\")": 0.6,
    "button:has-text(\"Notifications & Soun\")": 0.6,
    "button:has-text(\"Back to App\")": 0.7999999999999999,
    "button:has-text(\"Overview\")": 0.7999999999999999,
    "button:has-text(\"Feature Flags\")": 0.7999999999999999,
    "button:has-text(\"Monitoring\")": 0.7999999999999999,
    "button:has-text(\"Data Management\")": 0.7999999999999999,
    "button:has-text(\"Testing Tools\")": 0.7999999999999999,
    "button:has-text(\"Error Crawler\")": 0.7999999999999999,
    "button:has-text(\"Refresh\")": 0.7999999999999999,
    "button:has-text(\"Run Crawler\")": 0.7,
    "button:has-text(\"Dashboard\")": 0.7999999999999999,
    "button:has-text(\"Sign In\")": 1,
    "button:has-text(\"Stop\")": 0.6,
    "button:has-text(\"System Administrator\")": 0.7,
    "button:has-text(\"Pending\")": 0.6,
    "button:has-text(\"All Priority\")": 0.6
  },
  "testSuccessRates": {},
  "lastUpdated": "2026-01-25T09:13:15.747Z"
}